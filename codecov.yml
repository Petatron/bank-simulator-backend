github_checks:
  annotations: true
coverage:
  status:
    project:
      default:
        # basic
        target: 60%
        threshold: 0%
        base: 60%
        flags:
          - unit
        paths:
          - "src"
        # advanced settings
        branches:
          - main
        if_ci_failed: error #success, failure, error, ignore
        informational: false
        only_pulls: false